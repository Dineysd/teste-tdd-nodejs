describe('Authenticate', () => {
    it('A rota /authenticate deve retornar um token JWT valido',() => {

        const x = 2;
        const y = 4;

        const sum = x + y;

        expect(sum).toBe(6);
    });
});
